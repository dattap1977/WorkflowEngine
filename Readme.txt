In order to run this application execute the command
ant run
In order to run the unit tests execute the command
ant test

The application takes input from the files kept in the path src/com/inputFile

The output is being written to the console.

The build file is build.xml and the build properties file is workflow.properties

The unit test files are stored in the path src/test

The junit jar files are stored under the lib directory
